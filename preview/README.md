# preview/

외부에서 폰으로 확인하기 위한 **단일 파일** 빌드. 배포용이 아니다.

`site/` 의 CSS·JS·Work 데이터를 하나의 HTML 로 인라인해서, 서버 없이 파일만
열어도 레이아웃과 타이포를 볼 수 있게 한 것이다. 이미지·영상은 들어가지 않는다.

## 다시 만들기

`site/` 를 고친 뒤 아래를 실행하면 최신 내용으로 갱신된다.

```bash
python3 tools/build-preview.py
```

수동으로 손대지 마라 — `site/` 가 원본이고 이 파일은 항상 생성물이다.
