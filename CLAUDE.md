# CLAUDE.md — 라자냐 엠파이어 웹

이 저장소에서 작업할 때 먼저 읽는다.

## 이 저장소가 뭔가

라자냐 엠파이어(서울, 크리에이티브 프로덕션 스튜디오)의 웹.
두 가지가 들어 있다 — **섞지 마라.**

| 경로 | 무엇 |
|---|---|
| `site/` | **새로 만드는 사이트 (v3)**. 아직 배포 전 |
| `deploy/upload-to-www/` | **현재 운영 중인 lasagna.kr** 에 올리는 파일 |

`site/` 는 빌드 과정이 없는 순수 정적 파일이다. 프레임워크·번들러 없음.

```bash
npx serve site          # 로컬 확인 (file:// 로 열면 fetch 차단됨)
python3 tools/build-preview.py   # 폰으로 볼 단일 파일 미리보기 갱신
```

---

## 하드 룰

**포지셔닝** — "문제를 바라보는 방식부터 다시 디자인합니다."
`AI로 가장 빠르게` 는 **폐기했다.** 속도는 방어가 안 된다. 내부 미션으로만 쓴다.

**대외 카피에 가격을 언급하지 않는다.** 싸다/비싸다를 부정하는 문장도 가격 프레임이다.
내부 전략 문서는 직설적이어도 되지만 사이트 카피는 다르다.

**구조는 레이어링이다.** 라자냐 = 겹겹이 쌓기. 브랜드 문구가 네 층을 이미 이름 붙였다 —
**기술 · 감각 · 전략 · 실행**. 서비스를 나열하지 말고 이 네 층 아래로 묶는다.

**타입은 Inter + Noto Sans KR.** 둘 다 고딕(올고딕 룰 충족). 현재 라이브 사이트와 같은 조합이다.
**가짜 이탤릭 금지** — 한글에 진짜 이탤릭이 없다. 강조는 색값 단계로.

**컬러는 흑백 기조.** 액센트는 `base.css` 의 `--accent` 한 줄. `#F15A24` 확정 여부 미정.

**연락처는 `info@lasagna.kr`** — 개인 지메일 아니다.

---

## 케이스 페이지 규칙

**공개 승인은 별개다.** 캠페인 집행 승인 ≠ 스튜디오가 포트폴리오로 공개할 권한.
승인 전에는 각 페이지 하단 `<aside class="draft">` 에 미확인 항목을 적어두고,
승인 후 그 블록을 통째로 지운다.

- **제3자 브랜드 IP** — 클라이언트의 리테일 파트너·협업사 로고와 캐릭터는 승인 없이 사용 ✕.
  사실 서술(입점·협업)은 가능하되 이미지는 별개다.
- **의료** — 의료법 §56. 치료 결과·전후 비교·환자 사례 ✕
- **화장품** — 효능·성분 표현은 표시광고 규정 검토 후
- **상표 미확정 네이밍** — 클리어런스 전 노출 ✕

작성법은 `docs/CASE-PAGES.md`.

---

## 에셋

**원본은 `/Users/lasagna/Lasagna/Lasagna_Business/Client` 에 있고, 저장소에 넣지 않는다.**
기밀 자료와 대용량 원본이 섞여 있다. 선별·경량화한 사본만 `site/assets/` 로 들어간다.

```bash
cd site/assets
./collect-kv.sh                  # 프로젝트 폴더 목록
./collect-kv.sh --find <검색어>   # 이미지 후보 (해상도 포함)
./collect-kv.sh <파일> <id>       # 규격 맞춰 슬롯에 복사
./collect-kv.sh --check          # 채워진 슬롯 확인
./fetch-hero.sh <파일|URL>        # 히어로 영상 웹용 인코딩
```

`site/assets/kv/manifest.json` 이 Work 인덱스의 **단일 소스**다. `items` 가 렌더링되고
`_backlog` 는 보류 중인 프로젝트를 보존한다. `page: null` 이면 링크 없이 `준비 중` 칩만 붙는다.

**아직 채워지지 않았다** — 키비주얼·로고·히어로 영상 전부 자리표시자 상태.

---

## 현재 상태

**Work 4건** — LIE SANG BONG(케이스 ✅) · LIE(준비 중) · SYNOPEX(✅) · HARUHARU WONDER(✅)
백로그 5건은 `manifest.json` 의 `_backlog` 에 보존.

**운영 사이트** — HTTPS 적용 완료. `.htaccess` 는 이 호스팅(openresty/nginx)에서 안 먹어
`index.php` 로 서버 리다이렉트를 건다. 현재 302, 안정되면 301 로. `docs/CAFE24-HTTPS.md`.

**인증서 만료 2027-09-04** — 유료라 연 1회 갱신 필요.

---

## 문서

| 파일 | 내용 |
|---|---|
| `docs/PLAN.md` | **기획 v3** — 여기부터 읽는다 |
| `docs/CASE-PAGES.md` | 케이스 페이지 추가법 · 카피 원칙 · 컴플라이언스 |
| `docs/CAFE24-HTTPS.md` | 호스팅 현황과 해결책 |
| `docs/DEPLOY.md` | 배포 · 정적 호스팅 이전 검토 |
| `site/assets/README.md` | 에셋 드롭인 가이드 |

---

## 작업 태도

김률 디렉터는 **판단을 원한다.** 옵션 나열이 아니라 진단 → 소수의 날카로운 선택지 → 근거.
방향이 약하면 푸시백한다. 동의만 하는 답은 쓸모없다.

한국어로, 간결하게.
